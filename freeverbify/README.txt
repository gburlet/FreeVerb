README
------

type make

USAGE
-----
./freeverbify inputfile outputfile

To modify parameters, change these lines in the source code (FreeVerbify.cpp)
fv.setDamp(0.20);
fv.setWidth(0.5);
fv.setRoomSize(0.75);
fv.setMix(0.75);

Each should parameter should be [0,1]
